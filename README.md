# ZapLabels Bot ??

A Telegram bot for processing and editing shipping labels from DHL, DPD, Evri, and UPS.

## Features
- ?? Multi-carrier label processing (DHL, DPD, Evri, UPS)
- ?? Barcode & DataMatrix generation
- ?? PayPal payment integration
- ??? SQLite database support

## Setup
1. Clone the repo
2. Install requirements: `pip install -r requirements.txt`
3. Add your `.env` file with bot credentials
4. Run: `python bot.py`
